version: 0.1

phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
      - cd $CODEBUILD_SRC_DIR/iam-dk; npm install
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR/iam-dk; npm run build

artifacts:
  files:
    - 'build/*'

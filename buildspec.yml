version: 0.2

phases:
  install:
    runtime-versions:
        nodejs: 10
    commands:
      - cd $CODEBUILD_SRC_DIR/iam-dk; npm install
  build:
    commands:
      - cd $CODEBUILD_SRC_DIR/iam-dk; npm run build

artifacts:
  files:
    - 'iam-dk/build/**/*'
